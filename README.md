# NOTES

## Finished
Database is good 
Embeddings are good

## TO DO
Vector DB class needs to be designed better so it payload schema is not hardcoded
as well as adding indexing for certain features and filtering possiblities

After fixing the Vector DB class, finish the query method of the retrival service

Then you can work on the api

## NEW FEATURES
You can try to combine semantic search with term-based search using sparse vectors