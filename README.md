This project will be a helpful RAG model that will be able to answer questions using the LDS standard works

The index file includes most of the data, embeddings, and retrieval classes

The main.py file is just a fast api endpoint that allows for semantic search are query the top k results
